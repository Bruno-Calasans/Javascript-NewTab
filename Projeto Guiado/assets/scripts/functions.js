

    // funcções genéricas
    function isArray(dado){return dado instanceof Array}
    function isObj(dado){return dado instanceof Object}
    function isHTML(dado){return dado instanceof HTMLElement}


    export {isArray, isObj, isHTML}

